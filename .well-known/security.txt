---
permalink: /.well-known/security.txt
layout: none
sitemap: false
---
{%- comment -%}
  RFC 9116 security contact file — the canonical copy. Body is shared with the
  legacy /security.txt via the include; see the notes in it before editing.

  This directory starts with a dot, and Jekyll skips dotfiles by default, so
  `.well-known` must stay listed under `include:` in _config.yml or this file
  will never appear in _site.
{%- endcomment -%}
{%- include security-txt.html -%}
